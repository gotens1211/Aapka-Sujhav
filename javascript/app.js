//Animation at the start
var $bg=$('<div id="im"></div>');
$("body").append($bg);
$($bg).fadeOut(5000);

//Hide the menu bar when on a page other the home
//Append a button on the menu bar 
//Show the menu bar when clicked on the button
//Hide it again after some timeq